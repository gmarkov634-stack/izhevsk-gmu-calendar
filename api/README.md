# API / tenant runtime code

University-specific runtime adapters may be placed here as they are extracted from `kirov-gmu-calendar`.

Shared customer HTTP runtime, payments, subscriptions and trials must remain in `medical-calendar-core`.
